# Xcode
*.xcuserstate
xcuserdata/
.DS_Store

# Build artifacts
build/
*.profraw
DerivedData/

# Secrets & config
App/Internal/Keys.plist
App/Config/Keys.xcconfig

# Claude Code
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/worktrees/

# Project docs (local only)
ANALYSIS_AND_TODO.md
useful.md
Claude Code Insights.pdf
report.html

# Per-task TDD validation scratch script (CLAUDE.md "Test-Driven Implementation").
# Rewritten from scratch by each task, so tracking it made every parallel PR conflict on it.
# Durable invariants belong in their own named script (e.g. scripts/check_localization.sh).
scripts/validate_feature.sh
