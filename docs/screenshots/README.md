# Manual screenshots

These images are referenced by `docs/manual.md`. Captured from `4.0.0-beta31` and lightly processed (window shots are trimmed tight and given clean transparent rounded corners so they sit flush on the page).

| File | What it shows |
|------|---------------|
| `hero.png` | Lead image — the menu-bar item and the Daybreak popover together. |
| `menu-bar-item.png` | Starred cities in the macOS menu bar. |
| `popover.png` | The Daybreak popover: hero + city cards + footer. |
| `time-travel.png` | The scrubber mid-travel with the offset pill and "↺ Back to now". |
| `settings-cities.png` | Settings → Cities. |
| `settings-menubar.png` | Settings → Menu Bar (preset grid + live preview). |
| `settings-appearance.png` | Settings → Appearance (accent palette + PREVIEW card). |
| `settings-timetravel.png` | Settings → Time Travel. |
| `settings-general.png` | Settings → General (updates, beta, export/import, about). |

**Re-capture when the UI changes** (per CLAUDE.md, keep the manual current each release). Cleanest method: `⌘⇧4` → Space → Option-click the window for a transparent, shadow-free shot. Crop tight; keep file sizes modest.
